server = 000
client = 100
admin = 010

code = type*** if !first time an id will be assigned to it it and be from 12 to 16 int if code > 19 int {blacklisted}
rules: 
1: a server can't connect to a client 
2: admin can only be assigned from the server 
3: if a server connects to a server one of them have to be main 

